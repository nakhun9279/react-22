let Post;_1f0‍.x([["write",()=>write],["read",()=>read],["remove",()=>remove],["update",()=>update]]);_1f0‍.w("../../models/post",[["default",["Post"],function(v){Post=v}]]);


/*
  POST /api/posts
  {
    title: ‘제목’,
    body: ‘내용’,
    tags: [‘태그1’, ‘태그2’]
  }
*/
       const write = async ctx => {
  const { title, body, tags } = ctx.request.body;
  const post = new Post({
    title,
    body,
    tags,
  });
  try {
    await post.save();
    ctx.body = post;
  } catch (e) {
    ctx.throw(500, e);
  }
};




       const read = ctx => {};



       const remove = ctx => {};



       const update = ctx => {};